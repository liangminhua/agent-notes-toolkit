# AGENTS.md — Archived Agent Notes

Archived Agent Notes under the class directories are frozen historical snapshots, not current authority. Never edit, reformat, translate, repair, delete, or move a sealed file; use an active Agent Note or current documentation for new decisions and facts.

The archival change may only move the note, insert the `Archived: YYYY-MM-DD` line below `Status: implemented`, re-record the archive manifest (`an verify --write-archive`), and repair or delete inbound links. Do not inspect, verify, or repair links out of archived notes.
